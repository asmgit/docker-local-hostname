name: project_admin

# Reached from the host as http://project_admin.ldev and db.project_admin.ldev:5432
# Note: NO host port mappings. The name resolves to the container IP directly.
services:
  web:
    image: traefik/whoami
    hostname: project_admin.ldev

  db:
    image: postgres:18-alpine
    hostname: db.project_admin.ldev
    environment:
      POSTGRES_PASSWORD: secret
    volumes:
      - dbdata:/var/lib/postgresql

volumes:
  dbdata:
